{
  "name": "cognigy-extension-function-call",
  "version": "0.0.10",
  "description": "Cognigy.AI extension for function execution with input/output validation through flow calls",
  "main": "build/module.js",
  "scripts": {
    "transpile": "tsc -p .",
    "zip": "tar cfz $npm_package_name-$npm_package_version.tar.gz build/* package.json package-lock.json README.md icon.png",
    "build": "npm run transpile && npm run lint && npm run zip",
    "lint": "tslint -c tslint.json src/**/*.ts"
  },
  "keywords": [
    "Cognigy.AI",
    "extensions"
  ],
  "author": "Ben Elliot",
  "license": "ISC",
  "dependencies": {
    "@cognigy/extension-tools": "^0.16.1"
  },
  "devDependencies": {
    "@types/node": "^13.13.15",
    "tslint": "^6.1.3",
    "typescript": "^4.8.4"
  }
}
